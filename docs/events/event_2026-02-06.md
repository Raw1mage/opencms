#### 功能：系統清理與整合

**需求**

- 移除冗餘的 `DialogModelHealth` 組件，確認其功能已整合至 `DialogAdmin`。
- 釐清 "cli" 使用者來源。

**範圍**

- IN：移除 `src/cli/cmd/tui/component/dialog-model-health.tsx`。
- OUT：修改 "cli" 標記邏輯（除非使用者明確要求隱藏，目前僅作解釋）。

**方法**

- 刪除冗餘檔案。
- 確認無其他引用。

**任務**

1. [ ] 刪除 `src/cli/cmd/tui/component/dialog-model-health.tsx`。
2. [ ] 驗證 `DialogAdmin` 的 Activities 分頁功能完整。

**待解問題**

- 無。

#### 功能：antigravity 逐帳號配額顯示（Activities + Model Select）

**需求**

- Model Activities 顯示 antigravity 各帳號的獨立用量。
- Providers/Model Select 同步使用逐帳號用量。
- 若無配額資料，顯示 `--`。

**範圍**

- IN：更新 antigravity quota 查詢結果的對應方式（以帳號維度），並改用該映射顯示。
- OUT：不改變其他 provider 的用量邏輯。

**方法**

- 以 core account 的 refreshToken/email 對應 antigravity quota 結果，建立 `accountId -> quota groups` 映射。
- Activities 與 Model Select 皆改用該映射查詢配額。
- 無配額時回傳 `--`。

**任務**

1. [x] 產生 `accountId -> quota groups` 映射並接入 Activities。
2. [x] 同步 Providers/Model Select 的配額顯示邏輯。
3. [x] 更新 `docs/DIARY.md` 索引。

**待解問題**

- 無。

#### 功能：移除 Model Activities 用量「…」placeholder

**需求**

- 在 Admin Panel > Model Activities 列表中，取消顯示「…」placeholder。
- 對非 OpenAI（無用量值）不顯示任何多餘字樣。

**範圍**

- IN：調整 Activities 列表欄位寬度與標題字串長度。
- OUT：不更動其他頁面（Favorites / Providers）或全域 `DialogSelect` 行為。

**方法**

- 將 Activities 列表的 Model 欄位寬度縮減 1 字元，避免 `Locale.truncate` 截斷出現「…」。
- 同步調整 header 列字串長度，維持欄位對齊。

**任務**

1. [x] 調整 Activities 標題欄位寬度（model column -1）。
2. [x] 同步更新 header 列字串長度。
3. [x] 更新 `docs/DIARY.md` 索引。

**待解問題**

- 無。

#### 功能：rotation3d fallback 與 TUI 模型同步

**需求**

- TUI prompt 模型顯示需反映 rotation3d fallback 的實際模型。
- rotation3d fallback 需排除使用者已隱藏的模型與提供者。

**範圍**

- IN：prompt 模型同步、rotation3d 候選篩選、DIARY 索引。
- OUT：新增 SDK 事件型別或後端狀態欄位。

**方法**

- 以最新 assistant 訊息的 providerId/modelID 同步本地模型狀態（避免顯示與實際模型不同步）。
- 讀取 model.json 的 hidden/hiddenProviders，將其排除在 rotation3d fallback 候選之外。

**任務**

1. [x] prompt 同步 assistant 訊息模型。
2. [x] rotation3d 排除 hidden 模型與提供者。
3. [x] 更新 docs/DIARY.md 索引。

**待解問題**

- 無。

#### 功能：Model Activities 合併 Favorites + 分組呈現

**需求**

- Model Activities 欄位順序改為 Provider / Model / Account。
- 同一組 (Provider, Model) 多帳號時，只在首列顯示 Provider/Model，後續列只顯示 Account 並加上「∟」。
- 合併 Favorites：即使沒有活動資料也顯示，狀態欄顯示無狀態（-）。
- 移除 Favorites 子頁面。
- 依最近使用時間排序。

**範圍**

- IN：更新 Activities 資料組裝、欄位順序與顯示格式；移除 Favorites 分頁。
- OUT：不改變模型/帳號資料來源與收藏切換行為。

**方法**

- 以 `local.model.recent()` 建立 (Provider, Model) 活躍度排序鍵。
- Activities 以 (Provider, Model) 分組，Account 作為子列並加上「∟」前綴。
- Favorites 合併顯示，無活動資料的狀態欄輸出 `-`。
- 移除 Favorites 分頁與標題分支。

**任務**

1. [x] 調整 Activities 資料組裝與排序（Provider/Model 分組 + recent 排序 + account 子列）。
2. [x] 更新 Activities 欄位順序與 header 對齊（Provider / Model / Account）。
3. [x] 移除 Favorites 分頁（pages/title/options 分支）。
4. [x] 更新 `docs/DIARY.md` 索引。

**變更紀錄**

- 調整 Activities Account 欄位對齊：將「∟」拆為獨立符號欄，確保帳號文字起始對齊。
- 緊縮 Activities 欄寬與間隔：Provider/Model 欄縮窄，Account 欄加寬，欄間距維持 1 字元。
- Account 首列加「┬」，次列加「∟」，形成分枝視覺連結。

**待解問題**

- 無。

**變更紀錄**

- Activities 列表分支符號改為樹狀連線（├─/└─），並調整欄位寬度。
- Activities 首筆 account 分支符號改為「┬」，並將 account 欄位加寬 2 字元。
- Activities 頁移除 (C)lear 與 Hide 快捷鍵（含功能）。
- Activities 分支符號與 account 欄位貼齊，狀態欄位右移 1 格。
- Admin Panel Dialog 改用加寬尺寸，避免 Activities 欄位被截斷。
- Activities 列表改為不顯示省略號，並移除原因文字描述欄。
- Activities 欄寬改為依內容動態計算，並允許文字換行。
- Activities 用量/狀態欄改為貼近 account 欄（間隔 2 格），不再靠右。
- Admin Panel 視窗寬度改為 Activities 內容自適應，並新增 (S)ort 切換排序模式。
- Activities 單一帳號時不顯示分支符號。
- Activities 當前使用中的 model 行首新增 ✅。
- Activities 當前使用中帳號行尾加 ✅（避免影響對齊）。
- Activities ✅ 僅在 (provider, model, active account) 完整匹配時顯示，並縮小列 paddingRight。
- Activities 列 paddingRight 調整為 0。
- DialogSelect scrollbox paddingRight 改為 0，標題區 paddingRight 改為 2。
- DialogSelect scrollbox paddingRight 改為 1。
- DialogSelect scrollbox paddingRight 改為 0。
- Activities 自適應寬度取消額外緩衝（+0）。
- Activities 自適應寬度緩衝改為 +6。
- Activities 自適應寬度緩衝改為 +8。
- Activities 選擇模型後不關閉視窗，改為局部刷新。

#### 功能：貼上圖片流程 debug checkpoints

**需求**

- 在貼上圖片的關鍵流程加入 debugCheckpoint。
- 範圍包含：剪貼簿讀取、pasteImage 建檔、message/prompt 轉換、Read 工具呼叫。
- 記錄精簡欄位（type/path/mime/url 片段），避免輸出完整 base64。

**範圍**

- IN：`src/cli/cmd/tui/util/clipboard.ts`、`src/cli/cmd/tui/component/prompt/index.tsx`、`src/session/prompt.ts`。
- OUT：對話視窗輸出（僅寫入 debug.log）、web/app 端貼上流程。

**方法**

- 於四個關鍵節點新增 debugCheckpoint，僅記錄必要欄位與長度。
- data/url 僅保留前綴或長度，不記錄完整內容。

**任務**

1. [x] Clipboard.read 入口與結果（image/text/empty）。
2. [x] pasteImage 建立 FilePart 時記錄 source/path/url 概要。
3. [x] prompt.ts data:/file: 分支與 Read tool 呼叫前記錄。
4. [x] Read tool 失敗時補記錯誤與 filepath。

**變更紀錄**

- 新增 clipboard/paste/prompt 相關 debugCheckpoint，統一寫入 debug.log 並避免輸出完整 base64。

**待解問題**

- 無。
