#### 功能：測試驗證 (Protocol Selection)

**需求**

- 驗證 `Account.parseProvider` 能正確解析各類 Account ID。
- 驗證 Protocol 選擇邏輯（`getHeaderStyleFromUrl` 的模擬）是否嚴格遵循 Provider ID。

**範圍**

- `test/plugin/antigravity/protocol.test.ts`

**方法**

- 撰寫單元測試覆蓋：
  - `antigravity-subscription-*` ➜ `antigravity`
  - `gemini-cli-api-*` ➜ `gemini-cli`
  - `google-api-*` ➜ `google-api`
  - 協議映射邏輯：`antigravity` ➜ `antigravity`，其他 ➜ `gemini-cli`

**任務**

1. [x] 建立 `test/plugin/antigravity/protocol.test.ts`。
2. [x] 執行測試並確認通過。

**驗證**

- 測試通過 (7 pass)，確認邏輯正確無誤。
